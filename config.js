var config = {};


// Change these value here. 
//

config.waterTime = 20;   // water for xx seconds. 
config.pumpGPIO = 23;    // gpio pin on the. 
config.interval = 20;    // every xx min.

module.exports = config;